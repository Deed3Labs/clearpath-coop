import { Placeholder } from '@/components/dev/Placeholder';

export const metadata = { title: 'Clear Capital' };

export default function Capital() {
  return (
    <Placeholder
      rail="Capital / 01"
      title="Outside money sits here, and it does not sit on the members’ side."
    />
  );
}
